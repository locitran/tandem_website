import os 
import gradio as gr

def qa():
    gr.Markdown(
    """
    # Q & A

    ---

    ## ❓ What is a single amino acid variant (SAV)?

    A **single amino acid variant (SAV)** refers to a change in a protein's primary sequence caused by a nucleotide substitution
    that alters the encoded amino acid.

    The TANDEM webserver records each SAV using the **UniProt ID** followed by the amino acid substitution, for example:
    
    """
    )
    pass